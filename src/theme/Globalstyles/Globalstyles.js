import { createGlobalStyle } from "styled-components";

// Global styles and additional CSS reset's goes here 

export default createGlobalStyle`
  html {
    display: block;
    font-size: 13px;
    font-weight: 300;
    box-sizing: border-box;
    line-height: 1.15;
    font-family: sans-serif;
    color: #000;
    background: #fff;
  }
`;
